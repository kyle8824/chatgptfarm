const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const percent=v=>Math.max(0,Math.min(100,Math.round(Number(v)||0)));
function preference(p){const value=percent(p.value);return `<div class="interest-row${p.active?'':' future-interest'}"><div class="interest-label"><span>${esc(p.label)}</span><strong>${value}<small>/100</small></strong></div><div class="interest-track" role="meter" aria-label="${esc(p.label)} preference" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${value}"><i style="width:${value}%"></i></div><small>${!p.active?'Activity not yet available':p.satisfaction>=65?'Ready for some variety':p.satisfaction>=25?'Enjoyed recently':'Room for more'}${p.active&&p.minutes>0?` · ${p.minutes} min experienced`:''}</small></div>`;}
export function happinessMarkup(a){
 const h=a.happiness;if(!h)return '<p>Personal interests will appear with the next world update.</p>';
 return `<section class="happiness-detail" aria-label="Happiness and interests"><div class="mood-heading"><strong>${esc(h.feeling)}</strong><span>${percent(h.value)}/100 happiness</span></div><p>Happiness reflects wellbeing, comfort, company, enjoyment and a sense of progress.</p><div class="mood-factors">${h.factors.map(f=>`<span>${esc(f.label)} <b>${percent(f.value)}</b></span>`).join('')}</div><h3>What ${esc(a.name)} enjoys</h3><p class="interest-caption">Personal likes · 0–100. Favorites influence choices; doing the same thing repeatedly becomes less rewarding.</p><div class="interest-chart" aria-label="Activity preferences">${h.preferences.filter(p=>p.active).map(preference).join('')}</div>${h.preferences.some(p=>!p.active)?`<details class="future-interests"><summary>Interests for future activities</summary>${h.preferences.filter(p=>!p.active).map(preference).join('')}</details>`:''}${h.lastActivity?`<p class="recent-enjoyment">Last experienced: ${esc(h.lastActivity.label.toLowerCase())}.</p>`:''}</section>`;
}
