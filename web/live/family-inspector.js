const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function ageText(a){const l=a.life;if(!l)return '';return l.stage==='infant'?`${l.ageMonths} month${l.ageMonths===1?'':'s'} old`:`${l.ageYears} years old`;}
export function familyIntro(a){const l=a.life;if(!l)return '';return `<p class="life-summary">${ageText(a)} · ${esc(l.stage)}${l.partner?' · Partner: '+esc(l.partner.name):''}</p>${l.pregnancy?`<p class="expecting">Expecting a baby · ${Math.round(l.pregnancy.progress*100)}% through pregnancy</p>`:''}`;}
export function relationshipsMarkup(a,rate=6){
 const l=a.life,link=p=>`<button class="person-link" data-person="${esc(p.id)}">${esc(p.name)}</button>`,meter=(label,value,romantic=false)=>`<div class="relationship-meter${romantic?' romantic':''}"><label>${esc(label)}<span>${value}%</span></label><div class="bar" role="meter" aria-label="${esc(label)}" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${value}"><i style="width:${value}%"></i></div></div>`;
 return `${l?.pregnancy?`<div class="family-note"><strong>A baby is expected</strong><p>With ${link(l.pregnancy.otherParent)} · about ${Math.ceil(l.pregnancy.daysRemaining)} world days remaining.</p></div>`:''}${l?.parents.length?`<p>Parents: ${l.parents.map(link).join(' and ')}</p>`:''}${l?.children.length?`<p>Children: ${l.children.map(link).join(', ')}</p>`:''}${(a.relationships||[]).map(r=>`<section class="relationship-card"><div class="relationship-heading">${link(r)}<span>${esc(r.status)}</span></div>${meter('Trust',r.trust)}${meter('Familiarity',r.familiarity)}${meter('Affection',r.affinity)}${r.romance?meter(`${a.name}’s romantic feelings`,r.romance.toward,true)+meter(`${r.name}’s romantic feelings`,r.romance.from,true):''}</section>`).join('')||'<p>No relationships recorded yet.</p>'}${l?`<details class="aging-note"><summary>Age and life stages</summary><p>${l.yearDays} world days equal one year of age—${Number((l.yearDays/rate).toFixed(2))} real days at the current world speed. Babies depend on caregivers; children grow into adults. Friendship and romantic feelings develop separately.</p></details>`:''}`;
}
