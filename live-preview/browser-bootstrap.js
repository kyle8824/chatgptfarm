// Explicitly browser-hosted demonstration. Never writes to the production API.
window.CHATGPTFARM_RUNTIME_URL='https://isolated-browser-preview.invalid';
const originalFetch=window.fetch.bind(window),worker=new Worker('/live-preview/browser-worker.mjs',{type:'module'}),pending=new Map();let seq=0;
worker.onmessage=e=>{const p=pending.get(e.data.id);if(p){pending.delete(e.data.id);e.data.error?p.reject(Error(e.data.error)):p.resolve(e.data.world);}};
function request(event){return new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});worker.postMessage({id,event});});}
window.fetch=async(input,options)=>String(input).startsWith(window.CHATGPTFARM_RUNTIME_URL)?new Response(JSON.stringify(await request()),{headers:{'Content-Type':'application/json'}}):originalFetch(input,options);
addEventListener('DOMContentLoaded',()=>{const badge=document.createElement('aside');badge.id='livePreviewNotice';badge.innerHTML='<strong>ISOLATED LIVE PREVIEW</strong><span>Runs in this tab · 6× clock · NPC rules, no AI model</span><details><summary>What this proves / limitations</summary><p>Actions resolve as time passes. No future hour is precomputed. Closing this tab pauses this separate copy; production is untouched. The autonomous server version is built but not hosted yet.</p><button id="previewRain">Start rain now — preview test</button><p id="previewEventStatus" aria-live="polite"></p></details>';document.body.append(badge);document.querySelector('#previewRain').onclick=async()=>{await request({type:'rain'});document.querySelector('#previewEventStatus').textContent='Rain applied to the current world now.';};});
