# ChatGPTFarm deployment authority

The public world is the existing Cloudflare Worker `chatgptfarm`, Durable Object class `LiveValley`, binding `LIVE_VALLEY`, object name `live-valley-v1`. Its original creation identity is 1790030005263. Never rename, reset, reseed, copy over, or replace this world during a deployment or domain change. `world/state.json` is a legacy/export fixture, not the live authority.

The original `FarmWorld` / `WORLD` / `preview-v1` checkpoint and history are retained. `ORIGINAL_WORLD_ARCHIVED=true` prevents original-world advancement immediately and makes its alarms and scheduled recovery save a durable pause instead. When storage is quota-blocked that save is deferred; no catch-up or new simulation work runs in the original. Old setup start/resume requests cannot reactivate it. The original viewer is retained at the Worker's `/legacy/` path. The legacy GitHub writer remains disabled unless WORLD_RUNTIME is explicitly configured as github-legacy.

The Vercel `main` branch serves `chatgptfarm.com` and `www.chatgptfarm.com` through asset rewrites to the Worker's `/live/` viewer. The browser connects directly to `wss://chatgptfarm.kyle8824.workers.dev/live/ws` and reads public health/geometry from that same Worker. Vercel does not host the socket server or a second world. CORS permits these read-only spectator requests. Local development uses its local runtime, never the production world.

Cloudflare Workers Builds deploys `repair/continuous-runtime`; Vercel deploys `main`. Domain routing and runtime releases must be verified separately. Confirm the main-domain HTML/assets, `/live/health` build identity, and the direct WebSocket target. Preserve the existing namespaces, migrations, dashboard variables and secrets with `keep_vars`. A successful build during a storage outage does not establish that the saved world has resumed.

The server executes elapsed time on a 100 ms loop. Checkpoints and their wake-up alarms commit together, normally every 15 seconds, adjusting for size to target 30,000 routine storage rows written/day. AI budget reservations remain immediate durable writes. A crash can roll back progress since the last checkpoint; catch-up only processes elapsed time and reports lag. There is no precomputed future playback in LiveValley. A 15-minute recovery check wakes the same existing runtime after outages and retries the original world's archive pause.

The original buffered timeline and its bounded 256-transition diagnostic archive belong to the retired world. Do not describe that old execution model as the active LiveValley architecture, or claim permanent external archival exists. Verify the same world creation identity and retained villagers/structures after hosting permits reads. Tests and CI operate only on isolated fixtures.
