{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if git log -1 --pretty=%s | grep -q '^world: heartbeat '; then echo 'Ignoring canonical heartbeat deployment'; exit 0; fi; exit 1"
}
